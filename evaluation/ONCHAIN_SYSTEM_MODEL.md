# Onchain System Model

Identities: subnet owner, miner hotkey, validator hotkey.

Offchain: execution episodes → BMP → concealed execution → score.
Onchain: accepted validator weight vector.

**HTTP receipt ≠ useful memory ≠ accepted weight transaction.**

Before consequential submission, re-read mutable state: metagraph/UID-hotkey mapping, registration/stake/permit as applicable, weight rate limit, minimum weight count, max weight, version key and commit-reveal configuration.
